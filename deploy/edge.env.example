# Copy thành /srv/crm-koc/edge/.env rồi điền domain thật.
# Đây là file DUY NHẤT trên server phải sửa tay — stack app lấy .env từ
# GitHub Environment qua workflow deploy.

ACME_EMAIL=ops@example.com

PROD_APP_DOMAIN=example.com
PROD_API_DOMAIN=api.example.com

STAGING_APP_DOMAIN=stg.example.com
STAGING_API_DOMAIN=api.stg.example.com

STATUS_DOMAIN=status.example.com
